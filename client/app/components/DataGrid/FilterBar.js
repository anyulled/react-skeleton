import React from "react";

const FilterBar = () => (
    <div style={{width: 100 + "%", height: 40}}>
    	<table className="table">
    		<thead>
    			<tr>
    				<td style={{height:40}}>Filters</td>
    			</tr>
    		</thead>
    	</table>
    </div>
);

export default FilterBar;